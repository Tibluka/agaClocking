import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app.v7',
  appName: 'AgaClocking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
